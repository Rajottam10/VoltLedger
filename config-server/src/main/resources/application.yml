server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Rajottam10/voltledger-configs.git
          default-label: dev
          username: Rajottam10
          password: ${GITHUB_TOKEN}
          clone-on-start: true
